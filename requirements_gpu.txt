# Jane Street Market Prediction — GPU 训练依赖
# ============================================
# Python 3.10+
#
# 安装方式: 运行 bash setup_gpu.sh（自动检测 CUDA 版本）
# 或者手动:
#   1. nvidia-smi           # 看右上角 CUDA Version
#   2. pip install torch --index-url https://download.pytorch.org/whl/cu124  # CUDA 12.x
#      或
#      pip install torch --index-url https://download.pytorch.org/whl/cu118  # CUDA 11.x
#   3. pip install -r requirements_gpu.txt

# ---- 数据处理 ----
polars>=1.0.0
numpy>=1.24.0,<2.0
scipy>=1.10.0
pyarrow>=14.0.0

# ---- 机器学习 ----
scikit-learn>=1.3.0

# ---- CatBoost (GPU 版) ----
catboost>=1.2.0

# ---- XGBoost (GPU 版) ----
xgboost>=2.0.0

# ---- PyTorch (请手动安装，见上方说明) ----
# torch>=2.1.0

# ---- 工具 ----
psutil>=5.9.0
tqdm>=4.65.0
